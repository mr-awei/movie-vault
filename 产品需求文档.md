# 影海 Yinghai · 产品需求文档（PRD）

> 本地优先的影视收藏管理工具。不托管、不传播、不分发任何内容，所有数据保存在用户本机。

## 文档信息

| 项 | 内容 |
| --- | --- |
| 产品名称 | 影海 Yinghai（Movie Vault） |
| 文档版本 | v2.8.0 |
| 产品版本 | v2.8.0 |
| 文档状态 | 生效中 |
| 最后更新 | 2026-09-14 |
| 关联文档 | [技术设计文档.md](./技术设计文档.md) · [更新日志.md](./更新日志.md) · [交接文档.md](./交接文档.md) |

---

## 1. 背景与目标

### 1.1 背景
拥有大量本地影视收藏的用户普遍面临三个问题：

1. **文件夹即全部** —— 只有文件名，没有分类、简介、评分，无法像流媒体一样浏览；
2. **信息维护成本高** —— 手工整理 Excel / 命名规范难以长期坚持，元数据分散；
3. **隐私顾虑** —— 现有在线影库服务需要上传元数据、绑定账号，用户不希望私有收藏被采集。

### 1.2 产品目标
| 编号 | 目标 | 衡量方式 |
| --- | --- | --- |
| G1 | 把视频文件夹变成可浏览、可检索的海报墙影库 | 25 部以上影库可在 3s 内渲染出首屏 |
| G2 | 用一份 Excel 片单作为唯一权威分类来源 | 片单改动后重新对账即可全量生效 |
| G3 | 元数据自动补齐 + 本地永久缓存 | 一次抓取后离线可完整浏览详情 |
| G4 | 全程零上传、零账号、零埋点 | 网络请求仅发生在用户主动触发时 |

### 1.3 非目标（明确不做）
- 不提供任何视频内容的在线播放 / 下载 / 分发；
- 不做云端同步、不做账号体系、不做社交分享；
- 不内置内容源，不抓取受版权保护的内容本身；
- 不替代专业媒体服务器（如 Plex / Jellyfin）的多端串流能力。

---

## 2. 用户与场景

### 2.1 用户画像
| 画像 | 特征 | 核心诉求 |
| --- | --- | --- |
| 收藏型用户 | 本地影片数量 100–10000 部，已有整理习惯 | 快速浏览、分类清晰、封面美观 |
| 整理型用户 | 长期维护 Excel 片单 | 片单改动能被准确对账并体现 |
| 隐私敏感用户 | 不接受任何上传 / 账号 | 纯本地、可断网使用、数据可携带 |
| 展示型用户 | 家庭影院 / 大屏浏览 | 沉浸式海报墙、悬停预览、统计看板 |

### 2.2 典型场景
1. **首次建库**：选择视频根目录 + Excel 片单 → 扫描 → 对账 → 生成海报墙。
2. **片单更新**：编辑 Excel 后点击「扫描库」，分类 / 评分 / 简介自动刷新。
3. **批量补全**：对缺封面 / 缺详情的影片运行批量补齐，失败项单独重试。
4. **人工纠偏**：某条元数据识别错误 → 手工输入检索词 / ID 重抓。
5. **保护成果**：对已整理好的影片加锁，避免后续批量补齐覆盖。
6. **清理与维护**：识别「片单缺失」「文件未收录」，一键清理文件名广告。

---

## 3. 功能需求

> 优先级：P0 必须交付；P1 重要；P2 增强。

### 3.1 媒体库管理（FR-100）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-101 | 添加媒体库：指定名称、视频根目录、Excel 片单文件 | P0 | 目录不存在 / 片单格式错误时有明确报错 |
| FR-102 | 支持多媒体库，列表切换相互独立 | P0 | 切换后分类、统计、推荐均按新库刷新 |
| FR-103 | 扫描库：遍历目录建立影片记录（含扩展名过滤、最小体积过滤） | P0 | 扫描进度可见；重复扫描不产生重复记录 |
| FR-104 | 自动识别「重命名文件」并保留既有元数据 | P1 | 改文件名后重新扫描，元数据与封面不丢失 |
| FR-105 | 清理失效记录：文件已不存在的记录在扫描时自动移除 | P1 | 扫描后记录数与磁盘实际文件数一致 |
| FR-106 | 移除媒体库（受删除锁保护） | P1 | 需通过密码校验后执行 |

### 3.2 片单与对账（FR-200）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-201 | 解析 Excel 片单：`片名 / 分类 / 推荐评分 / 简介` | P0 | 兼容旧列名「标题 / 编号」，容错列顺序 |
| FR-202 | 额外列自动成为通用标签组，不预设维度 | P0 | 详情页按列名分组展示该列标签 |
| FR-203 | 对账：输出「片单缺失」与「文件未收录」两类差异 | P0 | 差异数量与明细可点击查看 |
| FR-204 | 未收录项可忽略（不再弹窗），也可取消忽略 | P1 | 忽略后仍保留在「未收录」分类中 |
| FR-205 | 一键清理文件名广告（预览 → 确认 → 执行） | P1 | 改名失败不丢元数据并给出原因 |
| FR-206 | 未配置片单时，按文件维度直接展示并自动按各源类型归类 | P1 | 不出现重复条目 / 重复 key |

### 3.3 浏览与检索（FR-300）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-301 | 海报墙三种视图：竖屏卡片、横屏卡片、纯文件名列表 | P0 | 切换视图不丢失筛选条件 |
| FR-302 | 三档海报密度 | P1 | 设置持久化，下次启动保持 |
| FR-303 | 虚拟滚动 | P0 | 5000 部影库滚动保持 60fps 体感，DOM 数量恒定 |
| FR-304 | 悬停预览面板（封面 + 标题 + 评分 + 标签 + 简介） | P1 | 悬停 1s 出现，移出自动关闭，滚动即关闭 |
| FR-305 | 搜索：标题 / 文件名 / 简介 / 标签 / 演员（大小写不敏感） | P0 | 输入即时过滤，无结果有占位提示 |
| FR-306 | 排序：添加时间 / 标题 / 年份 / 评分 / 最近播放 / 随机 | P0 | 升降序可切换 |
| FR-307 | 智能筛选：全部 / 收藏 / 最近播放 / 未评分 / 无封面 / 未收录 | P1 | 与多维筛选可叠加 |
| FR-308 | 多维筛选：标签 / 演员 / 制片公司 / 系列 / 分类 / 分辨率 / 时长 / 评分 / 年份 | P1 | 已选条件以 chip 展示，支持单个移除与全部清除 |
| FR-309 | 首页看板：Hero 推荐、每日推荐、全库随机、统计面板 | P2 | 推荐不重复，可手动刷新 |

### 3.4 元数据补齐（FR-400）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-401 | 五数据源自动降级：MovieDB → OMDb → OpenLibrary → JustWatch → 维基百科 | P0 | 命中即停，全程有事件日志 |
| FR-402 | 数据源可拖拽排序、单独启用 / 禁用 | P0 | 禁用源不发起任何请求 |
| FR-403 | 单源调试模式 | P2 | 可强制指定某一个源抓取 |
| FR-404 | 批量补齐：并发 / 间隔可配，支持暂停 / 继续 / 停止 | P0 | 暂停后不再发起新请求，停止后进度面板收起 |
| FR-405 | 失败明细：条目 + 原因，支持一键全部重试 | P0 | 重试仅针对失败项 |
| FR-406 | 手工输入检索词 / ID 重抓 | P1 | 识别失败时自动弹出入口，工具条常驻入口 |
| FR-407 | 详情页「按网址更新」：粘贴数据源页面链接直连抓取 | P1 | 非法链接给出明确提示 |
| FR-408 | 连续网络失败自动熔断该源 | P1 | 熔断后降级下一源并在日志标注 |

### 3.5 封面与截帧（FR-500）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-501 | 封面优先级链：手动 → 同目录同名图 → 数据源 → ffmpeg → 占位 | P0 | 列表与详情页使用同一优先级 |
| FR-502 | ffmpeg 兜底截帧：随机多点采样 + 质量评分 | P1 | 剔除过黑 / 过白 / 模糊 / 单调帧 |
| FR-503 | 预览集生成，详情页可查看、可放大、可设为封面 | P1 | 设为封面后列表立即刷新 |
| FR-504 | 重新截帧（单条 / 详情页） | P1 | 每次采样点不同，结果可覆盖 |
| FR-505 | 截帧失败打标并冷却，避免反复卡在坏文件 | P1 | 冷却期内批量任务跳过该文件 |

### 3.6 文件锁定（FR-600）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-601 | 单条锁定 / 解锁（卡片、右键菜单、详情页） | P0 | 锁定后卡片显示锁定徽标 |
| FR-602 | 多选批量锁定 / 解锁：全选、反选、取消选择 | P0 | 操作条实时显示已选数量 |
| FR-603 | 普通与强制批量补齐自动跳过锁定影片 | P0 | 进度面板显示「已锁定跳过 N 部」 |
| FR-604 | 补齐结束后报告被跳过的文件清单 | P0 | 弹窗逐条列出，可单条进详情、可全部解锁 |
| FR-605 | 详情页手动补齐不受锁定限制 | P0 | 锁定影片仍可手动更新信息 |
| FR-606 | 锁定状态不被扫描 / 对账的批量写盘覆盖 | P0 | 后台任务运行期间加锁不丢失 |

### 3.7 隐私与安全（FR-700）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-701 | 隐私护盾：一键模糊全部预览图，可默认开启 | P0 | 状态持久化，重启保持 |
| FR-702 | 删除锁：SHA-256 加盐密码，删除影片 / 移除库需校验 | P1 | 连续错误锁定并提示剩余次数 |
| FR-703 | 删除文件走系统回收站，目录仅含该文件时可连带删除 | P0 | 执行前展示预检结果并二次确认 |
| FR-704 | 代理配置：HTTP / HTTPS / SOCKS4 / SOCKS5 / 系统代理 | P1 | 提供连通性测试 |
| FR-705 | 卸载默认保留用户数据 | P1 | 卸载器提示并可选择删除数据 |

### 3.8 设置与更新（FR-800）

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| FR-801 | 外观：主题、海报密度、默认排序、列表展示模式 | P1 | 保存后立即生效并持久化 |
| FR-802 | 通用：外部播放器、ffmpeg 路径、语言、启动行为 | P1 | 路径无效时给出提示 |
| FR-803 | 数据源：API Key、顺序、启用状态 | P0 | Key 仅本地保存 |
| FR-804 | 检查更新：GitHub / Gitee，频率可配 | P2 | 展示版本、更新说明与下载入口 |
| FR-805 | 关于：版本、技术栈、第三方致谢、开源协议弹窗 | P2 | 协议可在应用内查看并复制联系邮箱 |

---

## 4. 信息架构

```
侧边栏
├── 首页（Hero 推荐 / 每日推荐 / 全库随机 / 统计）
├── 浏览（海报墙 / 文件名列表 / 多维筛选）
├── 智能分类（收藏 / 最近播放 / 未评分 / 无封面 / 未收录）
├── 媒体库列表（多库切换）
└── 设置 / 统计 / 关于
```

详情页信息优先级：标题 → 封面与预览 → 元数据（年份 / 时长 / 评分 / 分类 / 制片公司 / 系列 / 导演）→ 简介 → 标签 → 演职员 → 相关推荐。

---

## 5. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 5000 部影库首屏 ≤ 3s；滚动 60fps；批量补齐不阻塞 UI；大库写盘分段进行 |
| 可靠性 | 写盘为原子替换 + 防抖；异常中断最多丢失一个检查点批次；启动自动 schema 迁移 |
| 兼容性 | Windows 10+ / macOS 11+ / Linux（AppImage / deb）；中英双语；高分屏适配 |
| 隐私 | 无账号、无埋点；出网仅限用户主动操作；API Key 与密码仅存本地 |
| 可维护性 | 三层类型共享；`npm run typecheck` 全绿为合并门槛；文档与代码同步演进 |
| 可观测性 | 关键流程 console 打点；截帧过程写入独立日志文件便于排障 |

---

## 6. 验收标准（发布前必过）

1. 新建媒体库 → 扫描 → 对账 → 海报墙正确渲染，分类 / 评分 / 简介与片单一致。
2. 5000 部量级影库滚动、筛选、搜索无卡顿。
3. 批量补齐可暂停 / 继续 / 停止；失败项可重试；五源降级日志可见。
4. 锁定影片在普通与强制批量补齐中均被跳过，且结束后有清单提示。
5. 缺封面影片能通过 ffmpeg 截帧拿到封面与预览集。
6. 隐私护盾、删除锁、回收站删除、卸载保留守卫均可用。
7. 中英文界面切换无缺失文案；`npm run typecheck` 通过。

---

## 7. 度量指标

| 指标 | 目标 |
| --- | --- |
| 首次建库成功率（无片单用户同样可用） | ≥ 98% |
| 批量补齐命中率（至少一个源返回元数据） | ≥ 90% |
| 每千部补齐产生的失败明细条目 | ≤ 50 |
| 崩溃率（会话维度） | 0 |
| 用户主动触发之外的网络请求数 | 0 |

---

## 8. 版本规划

| 阶段 | 范围 | 状态 |
| --- | --- | --- |
| v2.0–v2.2 | 建库、对账、海报墙、基础补齐 | 已发布 |
| v2.3–v2.4 | 大库性能、虚拟滚动、截帧质量、批量任务可控 | 已发布 |
| v2.5–v2.6 | 标签分层、片单匹配增强、更新检查、卸载流程 | 已发布 |
| v2.7 | 双授权协议、协议弹窗、文档体系重写 | 已发布 |
| v2.8（规划） | 文件锁定体系完善、失效数据自愈、检索体验优化 | 开发中 |

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 数据源限流 / 反爬 | 补齐成功率下降 | 并发 + 间隔限速、连续失败熔断、多源降级、本地缓存复用 |
| 用户外部改名 / 移动文件 | 产生失效记录 | 扫描时按内容指纹识别重命名；自动清理失效记录 |
| 大库写盘耗时 | UI 卡顿 / 数据丢失 | 批量变更合并写入 + 分段检查点 + 原子替换 |
| 外接盘 / 网络盘离线 | 误判文件缺失 | 清理失效记录前要求本次扫描确实读到文件 |
| 片单格式不规范 | 无法对账 | 多列名兼容、列顺序容错、向导生成规范表头 |

---

## 10. 附录：术语表

| 术语 | 说明 |
| --- | --- |
| 媒体库（Library） | 一个视频根目录 + 一份 Excel 片单的组合 |
| 片单（Catalog） | Excel 文件，分类 / 评分 / 简介的权威来源 |
| 对账（Reconcile） | 将片单条目与磁盘文件比对，产出分类结果与差异 |
| 元数据（Meta） | 从数据源抓取的标题 / 年份 / 评分 / 类型 / 演职员 / 简介等 |
| 外部 ID（externalId） | 数据源侧的唯一标识（TMDb ID / IMDb ID / 作品 key 等） |
| 补帧（Frame fallback） | 无海报时用 ffmpeg 截取视频画面作为封面 / 预览 |
| 锁定（Lock） | 标记影片不参与批量补齐，防止人工整理成果被覆盖 |
