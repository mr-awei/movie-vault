# 更新日志（Changelog）

## [2.9.2] - 2026-09-16

**导出 Excel 完善 + 对账弹窗规范按钮 + 封面图一致性修复**

### 新增
- **对账弹窗规范按钮**：Step 2 添加「查看完整规范」和「打开规范文件位置」按钮，与新建片单向导保持一致
- **相关推荐 hover 标签**：hover 相关推荐卡片时，封面显示渐变遮罩 + 影片标签（品牌色 chip）
- **导出 Excel 单元格样式**：所有内容水平+垂直居中对齐，自动换行；切换到 xlsx-js-style 支持样式写入

### 变更
- **导出 Excel 表头**：从 6 列扩展到 9 列（编号/标题/年份/分类/推荐评分/简介/主题/地区/系列），与参考格式一致
- **导出 Excel 列宽**：标题列按最长标题（中文按2宽度计算）+ 6 字符空隙自适应，其他列设置合理宽度
- **相关推荐基本信息**：封面下方显示标题+年份+评分，不再只有封面

### 修复
- **封面图不一致**：首页横幅/列表卡片显示截帧图而详情页显示正确海报。根因是 resolveEntryPoster 优先级错误（截帧图排在详情封面之前），已调整为「手动封面 > 详情封面 > 截帧图 > posterPath」，全应用封面统一
- **导出 Excel 不生成文件**：XLSX.writeFile 在 Electron 主进程不可靠，改用 XLSX.write + writeFileSync；添加详细控制台日志方便排查
- **列表页 hover 预览弹窗被遮挡**：z-index 从 70 提高到 9999，卡片 hover 时 z-index 提升
- **hover 时长标签与预览面板重叠**：hover 卡片时自动隐藏时长小标签
- **对账弹窗 JSX 结构错误**：缺失 div 关闭标签导致 HMR 热更新 500 错误，代码无法更新

## [2.9.1] - 2026-09-16

**视频卡片时长标签可读性修复**

### 修复
- 时长标签在浅色/深色封面上看不清：经过多轮迭代，最终采用半透明黑底（60%）+ 背景模糊 + 白色文字 + 黑色文字阴影 + 紧凑尺寸的方案，确保在任何封面上都清晰可读且不突兀

## [2.9.0] - 2026-09-16

**播放列表全功能 + 多级标签拆分 + 侧边栏状态记忆**

### 新增
- **播放列表全功能**：创建/删除/改名、右键添加到列表、详情页"添加到播放列表"按钮、列表视图操作栏（添加影片/删除影片/播放全部）、拖拽排序、多选删除、已添加影片标注
- **多级标签自动拆分**："古装/欲望/夫妻" 自动拆分为 "古装"+"欲望"+"夫妻" 三个独立标签，筛选面板和详情页均使用拆分后标签，点击子标签筛选命中所有含该标签的视频
- **侧边栏状态持久化**：各分组（媒体库/筛选/我的清单/播放列表/待处理）的展开折叠状态、各分组内部滚动位置，自动保存到 localStorage，下次打开恢复
- **重复检测挪到顶部工具栏**：常用功能提升到扫描库旁边，hover 显示简介

### 变更
- **侧边栏布局调整**：播放列表、我的清单从筛选之前挪到筛选之后
- **侧边栏默认状态**：筛选默认展开，其余分组默认折叠
- **标签按钮样式改进**：增加间距、边框、优化宽度，更多标签同行显示
- **删除合集功能**：与播放列表功能重复，按用户要求移除

### 修复
- 播放列表大量 bug：新建后无法选择文件、列表展示全部影片、空列表无法添加文件、输入框偶发无法输入、点击选择立即取消、播放按钮无反应、PotPlayer 无断点续播、Sidebar playlists undefined 崩溃等
- 弹窗重叠问题
- 筛选折叠后下方多余空白问题

## [2.8.6] - 2026-09-15

**片单权威源接入 + 截帧算法重写 + 多项体验修复**

### 新增
- **Excel 片单作为唯一权威源**：片单中的「年份」「地区」「系列」三列正式接入，片单有的字段以片单为准，缺失字段才从各大数据源获取。详情页新增地区展示，系列优先展示片单值。
- **本地文件夹名作为唯一真名**：所有数据源搜索、海报抓取、兜底抓取、进度显示统一优先使用本地文件夹名（folderName），文件夹改名后媒体库自动同步。

### 变更
- **OMDb 搜索重构**：优先使用 `t` 参数精确搜索（不会 Too many results），失败后再用 `s`+年份兜底；中文短词直接放弃并提示，避免无效请求。
- **弹窗合并**：左下角「抓取过程」与右下角「正在扫描库」合并为统一进度面板，日志区域可折叠。
- **拖拽排序恢复**：数据源拖拽排序在所有模式下可用（之前仅 auto 模式显示）。
- **维基百科剧情提取**：从页面开头概述改为提取「剧情」章节正文，兼容 10 种章节命名（剧情/剧情简介/故事大纲/劇情等）。
- **截帧算法 2.0 随机化**：采样时间点加入 ±70% 随机偏移，选帧时每个时间段从前 3 个最佳帧中随机选取，确保每次重新截帧得到不同预览图。

### 修复
- **Excel 片单标题列误判**：表头第一列是「编号」时被误识别为标题列，导致 001/002 被当作影片名去匹配文件，全部显示"未收录"。现已优先匹配「片名/标题」列，「编号」仅作兜底。
- **网址更新后简介不刷新**：详情页渲染简介用 props 而非本地 state，网址更新后简介未同步；同时 backfillFromDetail 新增 description 回填。
- **重新截帧预览图不变**：前端版本号递增条件过严 + 选帧算法完全确定性 + ffmpeg output seeking 超时坏图，三层问题一并修复。
- **截帧产生坏图**：output seeking 速度过慢导致 8 路并发时后面的帧全部超时失败；回退为 input seeking，并发降至 4，超时增至 60 秒，且不再提前删除旧图。
- **wheel 事件报错**：图片放大浮层的滚轮切换用 React onWheel（默认 passive），调用 preventDefault 报错；改用 addEventListener({ passive: false })。
- **设为封面提示无效**：路径校验只允许 posters/ 目录，而预览帧在 preview-frames/ 目录下，导致全部被拒；现已同时允许两个缓存目录。

**待补充**：请在发布前把本次改动写进这一段（可重复执行本脚本，已存在时不会覆盖）。

## [2.8.3] - 2026-09-13

**待补充**：请在发布前把本次改动写进这一段（可重复执行本脚本，已存在时不会覆盖）。

## [2.8.2] - 2026-09-13

**待补充**：请在发布前把本次改动写进这一段（可重复执行本脚本，已存在时不会覆盖）。

## [2.8.1] - 2026-09-13

**待补充**：请在发布前把本次改动写进这一段（可重复执行本脚本，已存在时不会覆盖）。

本文件记录影海（Yinghai / Movie Vault）的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

变更类型：`新增` / `变更` / `修复` / `移除` / `安全`。

> 说明：2.7 之前的历史版本已按主题合并为「版本沿革」概览，仅保留对当前代码仍有参考价值的信息；当前实现的结构与接口以 [技术设计文档.md](./技术设计文档.md) 为准。

---

## [未发布]

### 修复
- **仓库链接更正**：关于弹窗的 GitHub / Gitee / 开源协议链接，以及「检查更新」使用的仓库地址，由 `yinghai-movie-vault` 更正为实际仓库 `mr-awei/movie-vault`（此前会导致链接 404、更新检查请求失败）。发布脚本与 GitHub Actions 工作流中的仓库地址同步更正。
- **CI 打包失败（证书与守卫脚本文件名不一致）**：GitHub Actions 的签名打包步骤引用 `build/yinghai-sign.pfx`，而仓库里只有影匣时代的 `build/yingxia-sign.pfx`，导致 electron-builder 直接报错退出；同样原因，`electron-builder.yml` / `installer.nsh` 引用的卸载守卫 `build/yinghai-uninstall-guard.ps1` 在仓库中不存在，守卫不会被装进安装包（表现为卸载勾选「删除用户数据」时提示未找到保护脚本、数据被保留）。现已：影海生成并使用**自己名下**的证书 `build/yinghai-sign.pfx`（`CN=影海 yinghai`，指纹 `50BAB905D077C2FA950CE0317D32624630451F9B`，密码走 GitHub Secret `CERT_PASSWORD`）；影匣的 `yingxia-sign.pfx` 原样保留；守卫脚本补齐同名副本（两份内容一致）；`.gitignore` 对两张证书均保留例外。
- **`scripts/gen-cert.ps1` 完全无法运行**：该脚本保存为 UTF-8 无 BOM，Windows PowerShell 5.1 按系统 GBK 解码中文导致引号不闭合、整个脚本解析失败。已改存为 UTF-8 带 BOM，并同步把 `scripts/sign.cmd` 的证书指纹指向影海证书。

---

## [2.8.0] - 2026-09-14

**文件锁定体系 + 数据自愈 + 文档体系规范化**

### 新增
- **文件锁定**：可对任意数量影片加锁，普通与强制批量补齐都会自动跳过；详情页手动补齐不受限制。锁定入口覆盖卡片悬停按钮、右键菜单、详情页按钮。
- **多选批量锁定**：浏览页新增多选模式，支持「全选当前 / 反选 / 取消选择」与「锁定选中 / 解锁选中」，底部操作条实时显示已选数量。
- **跳过报告**：批量补齐结束后弹出清单，逐条标注跳过原因（已锁定 / 文件不存在），支持单条进入详情与「全部解锁」。
- **运行时提示**：进度面板新增「已锁定跳过 N 部（不参与本次批量补齐）」实时提示。
- **批量锁定 IPC**：新增 `video:lockMany`，一次批量落盘，避免逐条全量写 `data.json`。
- **新增《通用影片评分与简介规范》v2.0**（中英双语，随应用打包）：十维加权评分、九档分档、锚点校准、全网评分体系对照、四段式简介与标签词表。
- **新增《技术设计文档》**（中英双语）：架构、模块、IPC 清单、存储设计、关键时序与安全设计。
- **新增《发布说明》**（中英双语）：版本号规范、构建签名、CI 发布、回滚流程。

### 变更
- **标题展示统一**：新增 `displayTitle()` 作为唯一展示标题来源（数据源标题 → 标题 → 文件名 → 条目名），首页 Hero、卡片墙、列表、详情、搜索与排序全部统一。
- **检索词统一**：单点补齐、批量补齐、海报抓取与后台兜底抓取统一优先使用已更新的标题，而不是首次导入的文件名。
- **抓取日志**：批量任务开始时清空上一轮日志，日志只反映本轮真实进度。
- **文档体系规范化**：中文版文档使用中文文件名（`产品需求文档.md` / `技术设计文档.md` / `交接文档.md` / `发布说明.md` / `更新日志.md`），英文版统一 `*.en.md`；开发者指南与专题文档归入 `docs/`。
- **文档全面重写**：README / 产品需求文档 / 技术设计文档 / 交接文档 / 发布说明 / 更新日志 按行业规范重写并保持中英双语同步，移除历史遗留内容。
- **片单向导对齐**：向导提示词与弹窗文案改用新评分规范口径（十维权重、九档分档、四段式简介与禁忌）。
- **关于弹窗**：产品描述与实际实现对齐（主题为深色 / 浅色 / 跟随系统）。

### 修复
- **锁定状态被覆盖**：扫描 / 对账的批量写盘不再清除用户锁定状态（写入方未显式携带 `locked` 时沿用既有值）。
- **锁定仍被补齐**：批量 worker 内增加二次跳过校验，锁定变更在任务运行途中同样生效。
- **失效记录累积**：扫描时自动清理「文件已不存在」的记录，避免记录数与磁盘实际文件数不一致。
- **批量补齐浪费请求**：跳过文件已不存在的记录，并在结果中返回明细。
- **按网址更新超时**：抓取超时时返回友好提示，不再向主进程抛出未捕获异常。
- **详情页演员区**：限制最大高度并出现滚动条，修复缺头像时色块与照片尺寸不一致、同名演员导致 React key 重复的问题。
- **文件名规范**：英文文档不再使用中文文件名，仓库内非 ASCII 文件名已清理。

---

## [2.7.0] - 2026-09-09

### 变更
- **许可证切换**：由 MIT 更换为「影海 双授权协议 v1.0」，`LICENSE` 完整替换为中英双语协议正文（非商业免费授权、恶意程序禁止条款、商业授权、权利保留、免责声明），原 MIT 协议备份为 `LICENSE.mit.backup`。
- **文档体系重写**：README / PRD / HANDOFF / PUBLISHING / CHANGELOG 及专题文档全面重写并保持中英双语同步。

### 新增
- **应用内协议弹窗 `LicenseModal`**：关于弹窗页脚新增「影海 双授权协议 v1.0 · 查看开源协议」入口，可就地查看完整协议正文（随界面语言切换），无需跳转外部浏览器。
- **商业授权邮箱一键复制**：协议弹窗底部展示 `new_mr_awei@163.com`，点击即复制并给出成功反馈。
- **i18n**：新增 `about.viewLicense`（中文「查看开源协议」/ 英文「View License」）。

### 修复
- 关于弹窗的许可证入口由外链改为应用内弹窗；`about.ts` 许可证名称与链接同步更新为仓库 `LICENSE`。

---

## [2.6.7] - 2026-09-02

### 变更
- **卸载流程整合**：设置页「卸载影海」改为单条流程 —— 应用内确认弹窗一次性选择「保留数据」或「删除数据」，通过参数直接传给 NSIS 卸载器，跳过标准欢迎页与独立的保留数据页。

### 修复
- 修复 NSIS 安装器构建失败：移除 `build/installer.nsh` 中引用未定义宏的冗余 `!undef`。
- 修复卸载时「无法清理用户数据、已保留数据」：卸载守卫脚本改以 **UTF-8 带 BOM** 保存，避免中文 Windows（GBK 代码页）下解析失败。
- 守卫脚本新增 `param([string]$DataDirOverride)`，正确接收 NSIS 传入的参数。
- 删除用户数据前先结束仍在运行的残留进程，释放 Electron 缓存 / Storage 文件锁。
- 删除重试次数 3 → 5，单次等待 800ms → 1.2s；失败提示补充可能原因。

---

## [2.6.6] - 2026-09-02

### 新增
- **截帧失败详情**：补齐后弹窗与详情页预览区双处展示失败张数、去重后的原因列表与处理建议。
- **Excel 片单向导对齐实际表结构**：向导提示词表头与真实 schema 一致，并新增「分类体系」段落。

### 变更
- **标签分层修正**：数据源类型标签写入独立的备用标签字段，与文档标签分离展示。
- **片单分类列**：分类列值走独立字段展示，不再混入标签分组。
- **类型标签清洗**：新增通用清洗器，去除数字后缀、括号与纯分隔符。
- 悬停放大延迟统一为 1s。
- 详情页自动截帧与手动「重新截帧」走同一套完整多帧流程。

### 修复
- 修复 tag 解析误把演员分组当作类型标签的问题。
- 修复文档标签与备用标签显示不一致的问题。

---

## [2.6.5] - 2026-09-01

### 新增
- **手工检索词 / ID 输入**：自动识别失败时自动弹出输入窗口，工具条另设常驻入口，可随时纠正错误元数据。
- **编辑标题同步改名**：`EditMetaModal` 提供「保存时同步修改文件名」开关（默认开），新增 IPC `video:renameFile` 与安全基名生成（去非法字符、保留名加前缀、超长截断）；改名失败不丢元数据，且保持视频 id 不变以维持封面 / 预览关联。

### 修复
- 修复 `data.json`「被外部修改」日志刷屏：高并发写盘期间跳过外部修改检测，重载后刷新 mtime 基准。
- 提升检索词提取鲁棒性，减少无效请求。

---

## [2.6.0] – [2.6.4] - 2026-09-01

### 新增
- 应用内检查更新（GitHub / Gitee），支持频率配置与更新说明展示。

### 修复
- 修复安装器启动崩溃（自定义语言对话框与窗口检测混用导致运行时异常），改用原生语言选择器。
- 安装器与卸载器文案跟随安装时选择的语言。

---

## [2.5.0] – [2.5.1] - 2026-09-01

### 新增
- 结构化标签（按 Excel 分栏分组）与备用标签分离展示。

### 修复
- 修复标签合并逻辑导致的显示不一致。

---

## [2.3.0] – [2.4.9] - 2026-08-30 ~ 2026-09-01

### 新增
- **虚拟滚动海报墙**：DOM 数量恒定，大库滚动流畅。
- **批量任务可控**：并发 / 间隔可配，支持暂停 / 继续 / 停止与失败重试。
- **截帧质量提升**：随机多点采样 + 质量评分，自动剔除过黑 / 过白 / 模糊 / 单调帧；新增预览集与「设为封面」。
- **统计面板**与首页推荐（Hero / 每日推荐 / 全库随机）。

### 变更
- 片单对账与文件对账合并为单次「扫描库」，进度条只显示一轮。

### 修复
- 修复大库批量写盘导致的 UI 卡顿与写盘死锁。
- 修复设置不持久化、对账卡死、封面兜底不一致等问题。

---

## [2.0.0] – [2.2.14] - 2026-08-29 ~ 2026-08-30

### 新增
- 媒体库（视频根目录 + Excel 片单）创建、扫描与对账。
- 海报墙三档密度、悬停简介、网格 / 列表切换、搜索与多维筛选。
- 五数据源自动降级抓取封面与详情，本地永久缓存。
- 隐私护盾、删除锁（SHA-256 加盐）、回收站删除、卸载数据保留守卫、代理配置。
- 中英双语界面、主题与密度设置。

### 变更
- 存储 schema 引入版本号与启动迁移，兼容旧 `data.json`。

---

## 版本沿革

| 阶段 | 主题 |
| --- | --- |
| v2.0 – v2.2 | 建库、对账、海报墙、基础元数据补齐 |
| v2.3 – v2.4 | 大库性能（虚拟滚动）、可控批量任务、截帧质量 |
| v2.5 – v2.6 | 标签分层、片单匹配增强、更新检查、卸载流程 |
| v2.7 | 双授权协议、应用内协议弹窗、文档体系重写 |
| v2.8（开发中） | 文件锁定体系完善、失效数据自愈、检索体验优化 |
