# 影海 Yinghai · 技术设计文档（TDD）

> 面向研发与维护者，描述当前实现的技术架构、模块职责、接口契约与关键流程。与代码同步演进，任何结构性变更都应回写本文。

## 文档信息

| 项 | 内容 |
| --- | --- |
| 文档版本 | v2.8.0 |
| 对应产品版本 | v2.8.0 |
| 最后更新 | 2026-09-14 |
| 关联文档 | [产品需求文档.md](./产品需求文档.md) · [交接文档.md](./交接文档.md) · [docs/](./docs) |

---

## 1. 技术目标与约束

| 目标 | 设计取舍 |
| --- | --- |
| 全本地、零上传 | 无服务端、无账号、无埋点；数据只写本机磁盘 |
| 大库流畅 | 渲染层虚拟滚动；主进程批量变更合并写盘 |
| 类型安全 | 全部 IPC 通道与载荷在 `src/shared` 定义，两端共享 |
| 可离线使用 | 元数据 / 封面 / 预览帧全部落本地缓存；ffmpeg 可捆绑 |
| 数据可演进 | 单文档 store + `schemaVersion` 原位迁移，向后兼容旧 `data.json` |
| 安全边界 | `contextIsolation` + preload `contextBridge`，渲染进程无 Node 权限 |

**环境**：Node.js 18+、Electron 31、electron-vite 2、TypeScript 5.5、React 18、Tailwind CSS 3。

---

## 2. 总体架构

### 2.1 进程模型

```
┌───────────────────────────────────────────────────────────────┐
│ Main process (Node)                                           │
│  ipc.ts ──► scanner / reconcile / fetch-meta / images / repo   │
│  store.ts（data.json）· proxy.ts（出网统一出口）                │
└───────────────▲────────────────────────────┬──────────────────┘
                │ ipcMain.handle             │ webContents.send
                │                            │ (scan:progress, poster:fetched)
┌───────────────┴────────────────────────────▼──────────────────┐
│ Preload (sandbox)                                             │
│  contextBridge.exposeInMainWorld('api', AppApi)               │
└───────────────▲────────────────────────────┬──────────────────┘
                │ window.api.xxx()           │ onScanProgress(cb)
┌───────────────┴────────────────────────────▼──────────────────┐
│ Renderer (Chromium, React)                                    │
│  App.tsx 状态中枢 · components/* UI · lib/api.ts 类型入口      │
└───────────────────────────────────────────────────────────────┘
```

### 2.2 分层职责

| 层 | 目录 | 职责 | 禁止 |
| --- | --- | --- | --- |
| 主进程 | `src/main` | 文件系统、网络、ffmpeg、持久化、全部副作用 | 直接操作 DOM |
| preload | `src/preload` | 白名单式暴露 `window.api` | 承载业务逻辑 |
| 渲染进程 | `src/renderer` | UI 与交互、轻量派生状态 | 访问 Node / fs |
| 共享 | `src/shared` | 类型、IPC 通道名、i18n、纯函数工具 | 引入 Electron 依赖 |

### 2.3 数据流

```
用户操作
  └─► window.api.xxx()            （preload 转发）
        └─► ipcMain.handle        （主进程处理器）
              ├─► repo/store      （内存 DB + 防抖原子写 data.json）
              ├─► scanner/reconcile（fs 遍历与比对）
              ├─► fetch-meta      （五源降级 + 代理 + 缓存）
              └─► images          （ffmpeg 截帧 + 海报缓存）
        ◄── 返回值 / scan:progress 事件
  ◄─ React setState 更新视图
```

---

## 3. 模块设计

### 3.1 主进程

| 模块 | 职责 | 关键导出 |
| --- | --- | --- |
| `lib/ipc.ts` | 注册全部 IPC handler；组装业务调用 | 无（副作用模块，`registerIpc()` 类入口） |
| `lib/scanner.ts` | 遍历目录 → 建立 / 复用影片记录；富集阶段生成封面 | `scanLibrary` `walk` `idForPath` `VIDEO_EXTS` |
| `lib/reconcile.ts` | 解析片单并与文件比对，产出 `ReconcileResult` | `reconcileLibrary` |
| `lib/fetch-meta.ts` | 五源降级调度、查询清洗、事件回调 | `fetchDetailSmart` `fetchPosterSmart` `DEFAULT_SOURCE_ORDER` |
| `lib/movie-db.ts` `omdb.ts` `openlibrary.ts` `justwatch.ts` `wikipedia.ts` | 各数据源适配器 | `fetchXxxDetail` `searchXxx` |
| `lib/images.ts` | ffmpeg / ffprobe 调用、质量评估、海报缓存 | `resolvePoster` `generatePreviewSet` `generateFrame` `postersCacheDir` |
| `lib/store.ts` | `data.json` 加载、schema 迁移、防抖原子写、外部修改检测 | `getDB` `saveDB` `SCHEMA_VERSION` |
| `lib/repo.ts` | 领域仓储（library / video / settings） | `listVideos` `updateVideo` `applyVideoChanges` `upsertVideo` |
| `lib/excel.ts` | 片单解析（首行找匹配列，容错列名与顺序） | `parseIntroExcel` 系列 |
| `lib/rename.ts` | 文件名广告清理与安全基名生成 | `cleanVideoFileName` `safeFileBaseName` |
| `lib/proxy.ts` | 统一出网（undici + socks）与连通性测试 | `httpFetch` `testProxyConnectivity` |
| `lib/ffmpegEnv.ts` `ffprobe.ts` | 可执行文件探测、技术参数读取 | `ffmpegAvailable` `probeVideo` |
| `lib/player.ts` | 调用系统默认播放器 / 外部播放器 | `openVideo` |
| `lib/runtime.ts` | 运行时路径（userData、日志路径等） | 路径工具 |
| `index.ts` | BrowserWindow 生命周期、单实例、托盘、自启 | 应用入口 |

### 3.2 渲染进程

| 模块 | 职责 |
| --- | --- |
| `App.tsx` | 应用状态中枢：库切换、筛选、排序、当前视图、批量任务、多选、弹窗编排 |
| `components/Sidebar.tsx` | 侧边栏导航、媒体库列表、智能分类、标签面板 |
| `components/HomeView.tsx` | 首页 Hero / 每日推荐 / 全库随机 / 统计入口 |
| `components/VirtualizedWall.tsx` | 虚拟化海报墙（按行计算偏移，仅渲染可视区 ± overscan） |
| `components/ListView.tsx` | 缩略图列表与纯文件名列表（含多选） |
| `components/EntryCard.tsx` | 卡片：封面优先级、徽标、悬停预览、右键菜单、多选勾选 |
| `components/VideoDetail.tsx` | 详情页：元数据、预览集、补齐 / 截帧 / 锁定 / 编辑 / 删除 |
| `components/BrowseBar.tsx` | 面包屑、智能筛选、排序、视图切换、多选入口 |
| `components/SettingsModal.tsx` | 设置中心（分类：常规 / 外观 / 网络 / 数据源 / 隐私 / 存储 / 更新 / 危险操作） |
| `components/ReconcileDialog.tsx` | 对账结果：缺失 / 未收录 / 忽略管理 / 改名预览 |
| `components/EditMetaModal.tsx` | 编辑标题、简介、标签，可选同步重命名磁盘文件 |
| `lib/util.ts` | 展示标题、封面解析、格式化、颜色等纯函数 |
| `lib/frameFallback.ts` | 列表懒加载单帧兜底 |

### 3.3 共享层

| 文件 | 内容 |
| --- | --- |
| `types.ts` | `Video` `Library` `MovieMeta` `Settings` `ReconcileResult` `DisplayEntry` 等核心类型 |
| `ipc.ts` | IPC 通道名常量表（唯一事实来源） |
| `api-types.ts` | `AppApi` 接口 + 各 IPC 的请求 / 响应类型 |
| `code.ts` | 文件名 → 标题 / 年份 / IMDb / TMDb ID 的解析 |
| `imdb` / 匹配工具 | `normTitleForMatch` `titleMatches` 等 |
| `i18n/` | 字典与 `t()` / `tMain()` 实现 |
| `about.ts` | 关于弹窗静态配置（版本、技术栈、第三方致谢、链接） |

---

## 4. IPC 接口清单

> 通道名定义于 `src/shared/ipc.ts`，载荷类型定义于 `src/shared/api-types.ts`。`invoke` 为请求 / 响应，`event` 为主进程主动推送。

| 通道 | 方向 | 入参 → 返回 | 说明 |
| --- | --- | --- | --- |
| `library:list` | invoke | `()` → `Library[]` | 媒体库列表 |
| `library:add` | invoke | `Omit<Library,'id'\|'createdAt'>` → `Library` | 新增媒体库 |
| `library:update` | invoke | `id, patch` → `Library \| null` | 更新媒体库 |
| `library:remove` | invoke | `id` → `void` | 移除媒体库 |
| `library:scanAndReconcile` | invoke | `libraryId` → `ReconcileResult` | 扫描 + 对账合并，单轮进度 |
| `library:reconcile` | invoke | `libraryId` → `ReconcileResult` | 仅对账 |
| `library:reconcileCache` | invoke | `libraryId` → `ReconcileResult \| null` | 读上次对账缓存 |
| `video:list` | invoke | `VideoFilter` → `Video[]` | 查询影片 |
| `video:get` | invoke | `id` → `Video \| null` | 单条影片 |
| `video:update` | invoke | `id, patch` → `Video \| null` | 更新字段（含收藏 / 锁定） |
| `video:lockMany` | invoke | `ids, locked` → `number` | 批量设置锁定，单次落盘 |
| `video:scan` | invoke | `libraryId` → `Video[]` | 仅扫描建记录 |
| `video:open` | invoke | `id` → `OpenResult` | 播放（系统 / 外部播放器） |
| `video:fetchDetail` | invoke | `id, idOverride?` → `FetchDetailResult` | 单点补齐信息 |
| `video:fetchByUrl` | invoke | `id, url` → `FetchDetailResult` | 按页面链接直连抓取 |
| `video:fetchPoster` | invoke | `id` → `Video \| null` | 抓取封面 |
| `video:generatePreviews` | invoke | `id` → 结果对象 | 重新截帧（封面 + 预览集） |
| `video:frameFallback` | invoke | `id` → `string \| null` | 单帧懒加载兜底 |
| `video:setPreviewAsCover` | invoke | `id, previewPath` → `Video \| null` | 预览帧设为封面 |
| `video:switchPoster` | invoke | `id, source` → `Video \| null` | 数据源图 ↔ ffmpeg 图切换 |
| `video:renameFile` | invoke | `id, newTitle` → `RenameFileResult` | 标题改动同步改名磁盘文件 |
| `video:probe` | invoke | `id` → `Video \| null` | ffprobe 读取技术参数 |
| `video:inspectForDelete` | invoke | `id` → 预检信息 | 删除前预检同目录文件 |
| `video:deleteFile` | invoke | `id` → 删除结果 | 移入系统回收站 |
| `library:fetchAll` | invoke | `libraryId, force?` → `BatchFetchResult` | 批量补齐（跳过锁定 / 失效） |
| `library:fetchPause` / `Resume` / `Stop` | invoke | `()` → `void` | 批量任务控制 |
| `library:batchProbe` | invoke | `libraryId` → 统计 | 批量补时长 |
| `library:previewRenames` | invoke | `libraryId` → `RenamePreviewItem[]` | 改名预览 |
| `library:applyRenames` | invoke | `libraryId, items` → 结果 | 执行改名 |
| `library:getCodes` / `library:exportCodes` | invoke | `libraryId[, fmt]` | 清单读取 / 导出 |
| `settings:get` / `settings:set` | invoke | `()` / `patch` → `Settings` | 读写设置 |
| `dialog:selectFolder` / `selectFile` | invoke | `()` → `string \| null` | 系统选择器 |
| `system:openPath` / `openExternal` / `revealInFolder` / `copyText` | invoke | 路径 / URL / 文本 → `void` | 系统能力 |
| `proxy:test` | invoke | `Settings` → 连通性结果 | 代理连通性测试 |
| `cache:clear` | invoke | `()` → `{ok,removed}` | 清理海报缓存 |
| `ffmpeg:status` | invoke | `()` → 状态 | ffmpeg 来源检测 |
| `lock:set` / `lock:verify` / `lock:delete` | invoke | `password` → 结果 | 删除锁管理 |
| `update:check` | invoke | `()` → 更新信息 | 检查更新 |
| `app:info` / `app:quit` / `app:uninstall` | invoke | — | 应用级能力 |
| `system:specGet` | invoke | `()` → 路径 | 内置规范文件路径 |
| `scan:progress` | event | `ScanProgress` | 扫描 / 对账 / 批量补齐进度与实时事件 |
| `poster:fetched` | event | `{videoId, posterPath, ...}` | 海报抓取完成，列表即时刷新 |

**约定**
1. 所有异步 handler 不向渲染进程抛未捕获异常；可预期失败以 `{ ok:false, error }` 返回。
2. `scan:progress` 的 `current` 使用「展示标题」（`meta.title` 优先），与 UI 展示保持一致。
3. 类型变更必须同时修改 `shared/types.ts` / `shared/api-types.ts`，靠 `typecheck` 兜底。

---

## 5. 数据存储设计

### 5.1 存储形态
单一 JSON 文档，位于用户数据目录：

```
<userData>/
├── data.json        # libraries / videos / settings / schemaVersion
├── posters/         # 封面与预览帧缓存（可安全删除）
└── *.log            # 运行时日志（如截帧日志）
```

### 5.2 结构与约束

| 字段 | 说明 |
| --- | --- |
| `schemaVersion` | 迁移版本号，低于常量 `SCHEMA_VERSION` 时启动执行原位迁移 |
| `libraries[]` | 媒体库：名称、根目录、片单路径、封面优先级、忽略的未收录路径 |
| `videos[]` | 影片：路径、文件名、标题、年份、标签、封面、时长、技术参数、`meta`、收藏、锁定 |
| `settings` | 用户设置（含 API Key、代理、隐私、锁哈希与盐） |

### 5.3 写盘策略
1. **防抖合并**：短时间内的多次写请求合并为一次落盘，避免高频全量写。
2. **原子替换**：先写临时文件再 rename；Windows 目标存在时先删除再 rename，跨设备时退化为直接写。
3. **分段检查点**：批量任务每处理 N 条落盘一次，异常中断最多丢失一个批次。
4. **批量变更合并**：`applyVideoChanges(changes)` 一次性应用并落盘，避免逐条全量写。
5. **自写检测**：写盘进行中跳过「外部修改」判定；重载后刷新 mtime 基准，避免把自己写的盘误判为外部编辑。

### 5.4 迁移机制
- 启动读取时若 `schemaVersion < SCHEMA_VERSION` 则执行迁移链，迁移完成后写回。
- 历史迁移覆盖：字段重命名（旧键 → `meta` / `externalId`）、标签分层（`tags` → `tagCategories` + `backupTags`）、分类列剥离（`introCategory`）、标题路径清洗等。
- 迁移必须幂等且可重入；迁移日志输出统计（touched / stripped / cleaned 等）。

### 5.5 对账缓存
对账结果额外写入磁盘缓存：启动 / 切库时先秒出缓存界面，再后台全量刷新，避免大库白屏。

### 5.6 锁定字段的保护
`applyVideoChanges` / `upsertVideo` 在写入方**未显式携带** `locked` 字段时，沿用库中已有的锁定值与时间戳；显式 `locked:false` 仍可正常解锁。该规则用于防止扫描 / 对账携带的旧副本覆盖用户锁定状态。

---

## 6. 关键流程

### 6.1 建库与扫描
```
选择目录 + 片单
  └─► scanLibrary
        ├─ walk 目录（扩展名白名单 + 最小体积过滤）
        ├─ 逐文件：
        │   ├─ 已存在（按路径）→ 复用
        │   └─ 新文件 → 按 contentHash / 标题+体积识别「重命名」→ 复用或新建
        ├─ 批量落盘（applyVideoChanges）
        ├─ 清理失效记录（path 不在本次扫描结果且文件不存在）
        └─ 富集阶段：并发解析封面（手动 / 同名图 / 占位，可选 ffmpeg）
```

### 6.2 对账
```
reconcileLibrary
  ├─ 解析片单（容错列名与顺序）
  ├─ 片单条目 × 磁盘文件 双向匹配（标题归一化 + 年份约束 + 词边界）
  ├─ 产出：matched / missing（片单有、文件无）/ unlisted（文件有、片单无）
  ├─ 未配置片单：按文件维度出条目，并按各源类型自动归类
  ├─ 清理死引用（失效 previewPaths）
  └─ 落盘 + 写对账缓存
```

### 6.3 批量补齐
```
libraryFetchAll(force)
  ├─ 过滤：locked 跳过、文件不存在跳过（明细随结果返回）
  ├─ worker 池（并发 N，每条间隔 M ms）
  │   ├─ 封面：缺封面 / 占位时抓取，失败走 ffmpeg 兜底
  │   ├─ 详情：缺失 / 陈旧（旧解析器 / 远程封面）时抓取
  │   └─ 同批检索词命中缓存 → 直接复用，节省请求
  ├─ 每 K 条落盘一次（CHECKPOINT）
  ├─ 支持暂停 / 继续 / 停止（共享状态对象）
  └─ 返回 { ok, failed, bySource, failures, lockedSkipped, missingSkipped, remainingNoPoster }
```

### 6.4 ffmpeg 截帧
```
generatePreviewSet
  ├─ 封面：随机 12 点采样 → 质量评估 → 选最优
  ├─ 预览：随机 22 点采样 → 取前 PREVIEW_COUNT 张
  ├─ 质量评估：缩放到 8×8 灰度，计算亮度均值与方差
  │    ok = 25 ≤ mean ≤ 230 且 variance ≥ 400
  ├─ 无候选通过 → 取方差最高者兜底
  └─ 清理临时候选文件，返回 { coverPath, previewPaths }
```
单帧与整片解码均设置超时并消费 `stderr`，防止损坏文件导致子进程挂死。

### 6.5 锁定
```
单条：toggleFlag(id,'locked') → video:update → 同步 reconcile 状态 + Toast
批量：多选 → video:lockMany(ids, locked) → 一次 applyVideoChanges
跳过：批量补齐前置过滤 + worker 内二次兜底
报告：结束后弹窗列出被跳过文件（已锁定 / 文件不存在），支持全部解锁
```

### 6.6 删除
```
video:inspectForDelete → 预检（目录内其他文件数）
  └─► 二次确认（受删除锁保护）
        ├─ 目录仅含该文件 → 连同目录一起移入回收站
        └─ 否则仅移动视频文件
              └─► 清理关联缓存 + 删除 data.json 记录
```

---

## 7. 元数据抓取设计

| 设计点 | 说明 |
| --- | --- |
| 源抽象 | 每源实现 `fetchXxxDetail(query, settings, onError?, manual?)`，统一返回 `MovieMeta \| null` |
| 检索词清洗 | `extractMovieQuery` 从文件名 / 手工输入提取干净标题 + 年份 + IMDb / TMDb 直连 ID |
| 降级顺序 | `customSourceOrder` 优先，否则 `DEFAULT_SOURCE_ORDER`；命中即停 |
| 禁用与熔断 | `disabledSources` 直接跳过；某源连续网络失败达阈值自动熔断本轮 |
| 事件回调 | 每次尝试推送 `{ code, src, status, detail }`，UI 实时展示降级过程 |
| 缓存 | 抓取结果写入 `video.meta` 并本地化封面；同批相同检索词走内存缓存 |
| 限速 | 批量并发与间隔可配；强制模式降并发、加大间隔 |
| 手工模式 | `manual=true` 时不做「从文件名猜 ID」，直接采用用户输入 |

---

## 8. 前端设计

### 8.1 状态管理
不引入第三方状态库。`App.tsx` 持有应用级状态（当前库、对账结果、筛选、排序、视图、批量任务、多选、弹窗），通过 props 下发；组件内状态（悬停、菜单、加载态）就地维护。派生数据统一用 `useMemo` 计算，避免重复遍历。

### 8.2 虚拟滚动
海报墙将「分类标题 + 卡片行」平铺为单一虚拟列表，预先计算每行 y 偏移与总高度，只渲染可视区 ± overscan 行。滚动事件用 `requestAnimationFrame` 节流。容器尺寸与最小列宽通过 `ResizeObserver` + `MutationObserver`（主题 / 密度切换）监听，列数随宽度自适应。

### 8.3 国际化
自研轻量字典（`src/shared/i18n`），主进程用 `tMain()`，渲染进程用 `t()`；缺键回退到 zh-CN 再回退到 key 本身，支持 `{var}` 插值。切语言通过 `setLocale` + 订阅机制驱动全局重渲染。

### 8.4 主题与密度
主题（深色 / 浅色 / 跟随系统）与海报密度（沉浸 / 标准 / 紧凑）通过根节点 class 切换，CSS 变量驱动；虚拟墙通过 `MutationObserver` 感知 class 变化并重新测量。

### 8.5 错误隔离
顶层挂载 `ErrorBoundary`，渲染异常时展示可恢复界面而非白屏；关键异步流程统一 `try/catch` 并降级为用户可读提示。

---

## 9. 安全设计

| 面 | 措施 |
| --- | --- |
| 渲染进程权限 | `contextIsolation` 开启，仅通过 preload 暴露白名单 API，无直接 Node 访问 |
| 出网 | 全部请求经 `lib/proxy.ts` 统一出口，支持 HTTP / HTTPS / SOCKS4 / SOCKS5 / 系统代理 |
| 密钥 | API Key 仅存本地 `data.json`，不上传 |
| 删除锁 | 随机盐 + SHA-256(password) 存储，校验通过才允许删除 / 移除库 |
| 删除安全 | 一律走系统回收站（可恢复）；执行前预检并二次确认 |
| 隐私护盾 | 一键模糊预览图，降低截图泄露风险 |
| 卸载 | NSIS + PowerShell 保留守卫，默认保留用户数据 |

---

## 10. 性能设计

| 场景 | 手段 |
| --- | --- |
| 大库渲染 | 虚拟滚动 + `contain` 布局隔离 + 懒加载图片 |
| 大库写盘 | 批量变更合并 + 分段检查点 + 防抖 + 原子替换 |
| 批量补齐 | 并发池 + 间隔限速 + 同检索词去重 + 兜底截帧限量 |
| 截帧 | 并发限制、超时 kill、失败打标冷却 |
| 网络 | 连接复用、缓存命中直接返回、可控熔断 |

---

## 11. 可观测性与排障

- 关键流程输出结构化 `console` 打点（扫描条数、对账差异、跳过原因、耗时）。
- 截帧过程单独写日志文件，便于定位损坏文件与超时。
- 前端 `ErrorBoundary` 捕获渲染异常并保留上下文。
- 常见问题定位路径见 [交接文档.md](./交接文档.md) 的排障清单。

---

## 12. 质量保障

| 环节 | 手段 |
| --- | --- |
| 静态检查 | `npm run typecheck`（主进程 + 渲染进程双 tsconfig），作为合并门槛 |
| 契约保障 | IPC 通道与载荷类型集中定义，签名变更即编译期暴露 |
| 手工验收 | 按 PRD 第 6 章「验收标准」逐条执行 |
| 回归重点 | 建库 / 对账、批量补齐可控性、锁定跳过、截帧兜底、删除与卸载保护 |
| 当前缺口 | 尚无自动化单元 / 端到端测试（见技术债） |

---

## 13. 构建与发布

```bash
npm run dev        # 开发
npm run typecheck  # 类型校验
npm run build      # 产物到 dist/
npm run pack       # electron-builder 安装包到 release/
```

发布流程、版本号同步、代码签名与回滚见 [发布说明.md](./发布说明.md)。

---

## 14. 技术债与演进

| 项 | 现状 | 计划 |
| --- | --- | --- |
| 自动化测试 | 无单测 / E2E | 引入核心纯函数单测与关键流程 E2E |
| 状态管理 | 单文件集中在 `App.tsx`，体量偏大 | 按领域拆分 context / reducer |
| 单文档存储 | 全量 JSON，超大库写放大 | 评估分片或嵌入式数据库 |
| 失效数据自愈 | 扫描时清理失效记录 | 增加可视化「数据体检」入口 |
| 截帧质量 | 基于亮度 / 方差的启发式 | 评估更精细的画面质量评分 |
