# 影海 Yinghai · 交接文档（Handoff）

> 面向接手维护的工程师：用最短路径建立对代码库的整体认知，并知道改动时该碰哪些文件。深度设计细节见 [技术设计文档.md](./技术设计文档.md)。

## 0. 速览

| 项 | 内容 |
| --- | --- |
| 产品 | 影海 Yinghai（Movie Vault）· 本地优先的影视收藏管理工具 |
| 当前版本 | v2.8.0 |
| 技术栈 | Electron 31 · electron-vite 2 · React 18 · TypeScript 5.5 · Tailwind CSS 3 · xlsx · undici |
| 数据存储 | 单文档 JSON（`data.json`）+ schema 迁移 + 防抖原子写 |
| 媒体处理 | ffmpeg / ffprobe（系统或捆绑） |
| 界面语言 | 简体中文 / English |
| 许可证 | 影海 双授权协议 v1.0（商业授权：`new_mr_awei@163.com`） |

> 免责声明：影海是本地文件整理工具，不托管、不传播、不分发任何视频内容。

---

## 1. 五分钟跑起来

```bash
npm install
npm run dev        # 开发（渲染进程 HMR，主进程改动自动重启）
npm run typecheck  # 提交前必跑
npm run build      # 构建产物到 dist/
npm run pack       # 生成安装包到 release/
```

首次启动建议：添加一个媒体库（视频目录 + Excel 片单）→ 点「扫描库」→ 检查海报墙与对账结果。

---

## 2. 目录地图

```
src/
├── main/
│   ├── index.ts              # 应用入口：窗口、单实例、托盘、自启
│   ├── assets/               # 随包的规范文档
│   └── lib/
│       ├── ipc.ts            # 所有 IPC handler（改动入口最集中）
│       ├── store.ts          # data.json 读取 / 迁移 / 防抖原子写
│       ├── repo.ts           # 领域仓储（library / video / settings）
│       ├── scanner.ts        # 目录遍历 → 影片记录（含失效记录清理）
│       ├── reconcile.ts      # 片单与文件对账、自动归类
│       ├── excel.ts          # 片单解析（容错表头与列顺序）
│       ├── fetch-meta.ts     # 五源降级调度、查询清洗、事件回调
│       ├── movie-db.ts / omdb.ts / openlibrary.ts / justwatch.ts / wikipedia.ts
│       ├── images.ts         # ffmpeg 截帧、质量评估、海报缓存
│       ├── ffmpegEnv.ts / ffprobe.ts
│       ├── proxy.ts          # 统一出网（含 SOCKS）
│       ├── rename.ts         # 文件名清理与安全基名
│       ├── player.ts         # 播放器调用
│       └── runtime.ts        # 运行时路径
├── preload/index.ts          # contextBridge → window.api
├── renderer/src/
│   ├── App.tsx               # 应用状态中枢（体量最大，改动需谨慎）
│   ├── lib/util.ts           # displayTitle / 封面解析 / 格式化
│   └── components/           # 24 个 UI 组件
└── shared/
    ├── types.ts              # 核心类型（Video / Library / MovieMeta / Settings）
    ├── ipc.ts                # IPC 通道名（唯一事实来源）
    ├── api-types.ts          # AppApi 与各通道载荷类型
    ├── code.ts               # 文件名 → 标题 / 年份 / ID 解析
    ├── i18n/                 # 字典与 t() / tMain()
    └── about.ts              # 关于弹窗静态配置
build/                        # NSIS 模板、卸载守卫脚本
scripts/                      # pack / publish / 证书
docs/                         # 专题文档
```

---

## 3. 核心概念

- **媒体库（Library）** = 视频根目录 + Excel 片单 + 封面优先级 + 忽略列表。
- **片单（Catalog）** = Excel，权威定义分类 / 推荐评分 / 简介；额外列自动成为标签组。
- **对账（Reconcile）** = 片单条目与磁盘文件双向匹配，产出 `matched / missing / unlisted`。
- **元数据（Meta）** = 从数据源抓取的标题 / 年份 / 评分 / 类型 / 演职员 / 简介；落盘在 `video.meta`。
- **externalId** = 数据源侧唯一标识（TMDb ID / IMDb ID / 作品 key）。
- **锁定（Lock）** = 标记影片不参与批量补齐（`video.locked`）。

---

## 4. 关键不变量（改代码前务必了解）

1. **IPC 契约集中在 `src/shared`**：新增 / 修改通道必须同时改 `ipc.ts` 与 `api-types.ts`，否则 `typecheck` 会失败。
2. **`scan:progress` 的 `current` 是展示标题**（`meta.title` 优先），不要退回用文件名，否则 UI 与列表不一致。
3. **批量写盘统一走 `repo.applyVideoChanges`**，不要逐条 `updateVideo`（会引发全量 JSON 写）。
4. **锁定字段受保护**：写入方未显式携带 `locked` 时沿用旧值；显式 `locked:false` 才解锁。
5. **失效记录在扫描时清理**：仅当本次扫描确实读到文件才执行，避免外接盘离线误删。
6. **删除一律走系统回收站**，且受删除锁保护。
7. **所有出网请求必须走 `lib/proxy.ts`**，否则代理设置会被绕过。
8. **schema 变更必须升 `SCHEMA_VERSION` 并补迁移**，迁移需幂等。

---

## 5. 常见改动怎么做

| 需求 | 改动位置 |
| --- | --- |
| 新增数据源 | `shared/types.ts`（SourceId）→ `main/lib/<source>.ts` → `fetch-meta.ts` → `SettingsModal.tsx` → 两份 i18n。详见 [docs/新增数据源使用说明.md](./docs/新增数据源使用说明.md) |
| 新增设置项 | `shared/types.ts` 的 `Settings` + `DEFAULT_SETTINGS` → `SettingsModal.tsx` → 两份 i18n |
| 新增 IPC | `shared/ipc.ts` → `shared/api-types.ts` → `preload/index.ts` → `main/lib/ipc.ts` → 渲染层调用 |
| 调整卡片 / 列表样式 | `components/EntryCard.tsx` / `ListView.tsx`；展示标题统一用 `lib/util.ts#displayTitle` |
| 调整截帧策略 | `main/lib/images.ts`（采样数量、质量阈值、超时） |
| 调整批量任务行为 | `main/lib/ipc.ts#libraryFetchAll` + `App.tsx` 的进度 / 失败 / 跳过编排 |

---

## 6. 排障清单

| 现象 | 排查方向 |
| --- | --- |
| 页面白屏 / 报 `is not defined` | 多为热更新加载了改动中间态；刷新或重启应用。若持续，检查 `App.tsx` 是否有残留旧变量名 |
| 批量补齐卡住不动 | 查看进度面板与主进程日志；确认是否有损坏视频导致 ffmpeg 超时（应已 30s 超时 + 失败打标） |
| 列表仍显示旧文件名 | 检查是否使用了 `displayTitle`；主进程 `current` 是否传了展示标题 |
| 锁定后仍被补齐 | 确认锁定已落盘（数据目录 `data.json` 中 `locked:true`）；确认主进程已重启；查看日志中「跳过 N 部锁定」 |
| 影库数量比磁盘多 | 记录中含失效项，执行一次「扫描库」即自动清理 |
| 补齐失败率高 | 检查 API Key、代理连通性与数据源顺序；失败明细弹窗可逐条查看原因 |
| 封面糊 / 黑 | 检查 ffmpeg 是否可用（设置页状态）；详情页可重新截帧 |
| 设置不生效 | 确认写盘完成（防抖写），必要时重启应用 |

---

## 7. 数据位置

| 系统 | 路径 |
| --- | --- |
| Windows | `%APPDATA%/local-movie-vault` |
| macOS | `~/Library/Application Support/local-movie-vault` |
| Linux | `~/.config/local-movie-vault` |

- `data.json`：媒体库、影片、设置。
- `posters/`：封面与预览帧缓存（可安全删除）。

---

## 8. 交接验收清单

- [ ] `npm run dev` 可正常启动并渲染已建好的影库。
- [ ] `npm run typecheck` 全绿。
- [ ] 新建媒体库 → 扫描 → 对账 → 海报墙正确。
- [ ] 批量补齐可暂停 / 继续 / 停止，失败可重试。
- [ ] 锁定影片在普通与强制补齐中均被跳过，并有跳过清单。
- [ ] 缺封面影片可通过 ffmpeg 截帧拿到封面与预览。
- [ ] 隐私护盾、删除锁、回收站删除、卸载保留守卫可用。
- [ ] 中英文切换无缺失文案。
- [ ] 已阅读 [技术设计文档.md](./技术设计文档.md) 与 [产品需求文档.md](./产品需求文档.md)。

---

## 9. 联系与发布

- 商业授权 / 合作：`new_mr_awei@163.com`
- 仓库：GitHub `mr-awei/movie-vault`，同步 Gitee 同名仓库
- 发布流程见 [发布说明.md](./发布说明.md)
