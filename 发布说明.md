# 影海 Yinghai · 发布说明（Publishing）

> 面向发布负责人：版本号规范、构建与签名、CI 发布、回滚与核查清单。

## 1. 前置条件

| 项 | 说明 |
| --- | --- |
| Node.js | 18+（建议与 CI 使用同一大版本） |
| 依赖 | `npm install` 已完成 |
| Windows 代码签名 | 证书已随仓库提供（`build/yinghai-sign.pfx`，`.gitignore` 为其保留例外）；密码通过环境变量 / CI Secret `CERT_PASSWORD` 提供，不落仓库 |
| GitHub 发布凭据 | CI 自动发布**无需配置**（`GITHUB_TOKEN` 由 Actions 注入）；仅本地手动发布需要环境变量 `GITHUB_TOKEN` |
| Gitee 同步 | GitHub 仓库 Secret `GITEE_TOKEN`（Gitee 私人令牌），配置方法见 4.4 |

## 2. 本地构建与打包

```bash
npm run typecheck      # 发布前必跑，全绿才继续
npm run build          # 构建产物到 dist/
npm run pack           # 执行 scripts/pack.mjs → electron-builder → release/
```

`scripts/pack.mjs` 产出：

- `*.exe` —— NSIS 安装器（含中英文语言选择、运行中检测、卸载数据保留守卫）
- `*.7z` / `*.zip` —— 免安装压缩包
- `*.blockmap` + `latest.yml` —— 自动更新元数据

> 输出目录由 `electron-builder.yml` 的 `directories.output` 决定（默认 `release/`）；可用环境变量 `yinghai_PACK_OUT` 覆盖。

## 3. 版本号规范

采用语义化版本 `MAJOR.MINOR.PATCH`：

| 变更类型 | 递增位 | 示例 |
| --- | --- | --- |
| 不兼容变更 / 大版本 | MAJOR | 2.x → 3.0.0 |
| 向后兼容的新功能 | MINOR | 2.7.0 → 2.8.0 |
| 向后兼容的问题修复 | PATCH | 2.7.0 → 2.7.1 |

**发布前需同步两处：**

1. `package.json` → `version`（应用版本号唯一来源，关于弹窗与实际安装版本均读取此处）
2. `更新日志.md` / `CHANGELOG.en.md` → 顶部新增条目（将 `[未发布]` 改为 `[x.y.z] - YYYY-MM-DD`，并补一个新的 `[未发布]` 段落）

可选同步：文档信息表中的「文档版本 / 产品版本」（`产品需求文档.md`、`技术设计文档.md`、`交接文档.md` 及对应英文版）。

## 4. 代码签名与发布凭据

> **仓库中的两张 pfx（各自独立，勿混淆）**
>
> | 文件 | 归属 | 证书主体 | 说明 |
> | --- | --- | --- | --- |
> | `build/yinghai-sign.pfx` | 影海（本仓库 CI 签名） | `CN=影海 yinghai, O=yinghai, C=CN`（2031-09-14 到期） | workflow 的 `certificateFile` 指向它；**GitHub Secret `CERT_PASSWORD` 必须是这张证书的密码** |
> | `build/yingxia-sign.pfx` | 影匣（独立项目，仍在更新） | `CN=影匣 YingXia, O=YingXia, C=CN`（2031-08-27 到期） | **不要重命名或删除**，那边仍在使用 |
>
> 影海已于 2026-09-14 生成自己名下的证书，指纹 `50BAB905D077C2FA950CE0317D32624630451F9B`，本地签名脚本 `scripts/sign.cmd` 的 `TP` 已同步。
> ⚠️ **换证书 = 换密码**：重新生成后旧密码对 `yinghai-sign.pfx` 失效，必须同步更新 GitHub Secret `CERT_PASSWORD`，否则 CI 的签名步骤会直接失败。

### 4.1 生成自签名证书（首次执行一次即可）

在项目根目录用 **PowerShell** 执行：

```powershell
# 1) 先设密码：这个密码稍后要填进 GitHub Secret，务必先记牢
$env:CERT_PASSWORD = '换成你的强随机密码'

# 2) 生成证书并导出 pfx（CN=影海 yinghai，有效期 5 年）
powershell -ExecutionPolicy Bypass -File .\scripts\gen-cert.ps1
# → 生成 build\yinghai-sign.pfx，并打印证书 Thumbprint

# 3)（可选）让本机不再弹 SmartScreen 警告：把证书导入「受信任的根证书颁发机构」（需管理员）
Import-PfxCertificate -FilePath .\build\yinghai-sign.pfx `
  -CertStoreLocation Cert:\LocalMachine\Root `
  -Password (ConvertTo-SecureString $env:CERT_PASSWORD -AsPlainText -Force)
```

随后把证书提交进仓库（`.gitignore` 已为它保留例外，否则 `*.pfx` 会被忽略）：

```bash
git add build/yinghai-sign.pfx
```

> 生效条件：**证书文件在仓库里** + **密码在 CI Secret 里**。
> 自签名证书不能让 SmartScreen 警告消失（除非用户本机导入信任），它只保证「安装包确实由本项目的证书签名」。

### 4.2 校验现有 pfx 的密码（忘记密码时）

```powershell
# 把 <候选密码> 换掉；能打印出 Subject 说明密码正确，抛异常说明密码错误
$p = ConvertTo-SecureString '<候选密码>' -AsPlainText -Force
$c = New-Object System.Security.Cryptography.X509Certificates.X509Certificate2 (Resolve-Path .\build\yinghai-sign.pfx), $p
$c.Subject
```

密码彻底想不起来 → 按 4.1 重新生成一份覆盖 `build/yinghai-sign.pfx`，提交后把 Secret 换成新密码。

### 4.3 GitHub 仓库 Secrets

仓库页 → **Settings → Secrets and variables → Actions → New repository secret**，新增两条：

| Secret | 值 | 用途 |
| --- | --- | --- |
| `CERT_PASSWORD` | 4.1 生成 pfx 时设置的密码 | workflow 以 `CSC_KEY_PASSWORD` 注入，electron-builder 用它给安装器签名 |
| `GITEE_TOKEN` | Gitee 私人令牌（见 4.4） | 发布后创建/更新 Gitee Release；不配则只打警告并跳过该步，GitHub Release 照常发布 |

`GITHUB_TOKEN` 由 Actions 自动注入，无需手工配置（workflow 已声明 `permissions: contents: write`）。
改完 Secret 不需要重跑历史 run，下一次打 tag 自动生效。

### 4.4 Gitee 侧要做的事

1. 确保 Gitee 上已有同名仓库（本项目为 `gitee.com/mr-awei/movie-vault`）。
2. 生成**私人令牌**：右上头像 → 设置 → 安全设置 → 私人令牌 → 生成新令牌，勾选权限：
   - `projects`（仓库读写，创建 Release 必需）
   - `notes`（workflow 同步步骤所需）
3. 复制令牌，填进 4.3 的 GitHub Secret `GITEE_TOKEN`（令牌本身只存 GitHub Secret，不落仓库）。
4. Gitee **没有 CI**：无需在 Gitee 配置任何 Secret，也无需在 Gitee 打包。安装包由 GitHub Actions 构建并托管在 GitHub Release；Gitee Release 只写入下载链接（Gitee 附件上限 100MB）。

### 4.5 不配置证书会怎样

workflow 用 `--config.win.certificateFile=build/yinghai-sign.pfx` 指定证书，**文件不存在会让 electron-builder 直接报错退出**（v2.8.0 那次 tag 发布失败的根因就是文件名不一致）。
若确实要暂时跳过签名：注释掉 workflow 中该参数与 `CSC_KEY_PASSWORD`，**不要**只删除 pfx 文件。

## 5. 发布流程

### 5.1 自动发布（推荐）

1. 在 `main` 上完成功能与文档更新，`npm run typecheck` 全绿。
2. 更新版本号 + 更新日志（见第 3 章）。
3. 一键提交并双端推送：

```bash
node scripts/publish.mjs 2.8.1     # bump 版本 → 提交 → 打 tag vX.Y.Z → 推 Gitee + GitHub
```

4. GitHub 侧收到 tag 触发 Actions（`.github/workflows/release.yml`）：

   `Typecheck` → `Build` → **签名打包**（`release/*.exe`）→ 上传到 GitHub Release（正文取「更新日志.md」顶部）→ 用 `GITEE_TOKEN` 创建/更新 Gitee Release。

### 5.2 手动发布（不走 CI）

```bash
GITHUB_TOKEN=ghp_xxx TAG=v2.8.1 node scripts/publish-release.mjs
```

`scripts/publish-release.mjs` 直接调 GitHub API 创建 Release 并上传 `release/` 下的产物，适合本地已 `npm run pack` 出包的场景。

### 5.3 双端分工

| 端 | 做什么 |
| --- | --- |
| GitHub | 托管代码 + Actions 构建/签名/发版 + 托管安装包（`scripts/publish.mjs` 推代码与 tag） |
| Gitee | 镜像代码（`scripts/publish.mjs` 同推 main + tag）；**Release 由 GitHub Actions 调 Gitee API 创建**，正文写入 GitHub 下载链接。Gitee 侧无需任何 Secrets |

若 Gitee Release 同步失败：确认 4.3 的 `GITEE_TOKEN` 有效且包含 `projects` 权限，然后重跑该 workflow（Actions → 失败的那次 run → Re-run failed jobs）。

## 6. 发布后核查

- [ ] Release 页面包含安装器与压缩包，`latest.yml` 已上传。
- [ ] 安装器可正常安装；语言选择、运行中检测、卸载保留数据均正常。
- [ ] 安装后应用可启动，版本号与发布版本一致。
- [ ] 应用内「检查更新」能检测到新版本。
- [ ] 中英文界面文案完整。
- [ ] 更新日志内容与实际变更一致。

## 7. 回滚

1. 若仅元数据问题：修正 Release 说明或重新上传产物，无需改版本号。
2. 若产物有缺陷：版本号 **只能前进不能回退**，发布一个修复补丁版本（PATCH）。
3. 若需临时下架：将 Release 设为 Draft 或删除 Release（保留 tag 以便追溯）。
4. 回滚后务必在「更新日志.md」中记录原因与影响范围。

## 8. 注意事项

- `npm run` 中**没有** `publish` 脚本，发布统一走上述流程。
- 安装器内置卸载数据保留守卫（`build/yinghai-uninstall-guard.ps1`，由 `electron-builder.yml` 的 `extraResources` 打包进 `resources/`），该脚本必须保持 **UTF-8 带 BOM** 保存，否则中文 Windows 下会解析失败。
- 守卫脚本目前与影匣时代的 `build/yingxia-uninstall-guard.ps1` **并存（内容相同）**：打包只引用 `yinghai-` 那个，另一个留给影匣项目；修改守卫逻辑时**两份都要同步**，或待影匣迁移完成后删除旧名文件。
- 改名类改动（证书、守卫等资源）务必同步 `.gitignore`、`electron-builder.yml`、`installer.nsh`、workflow 与本文件，**且确认无其他并行项目仍引用旧文件名**。
- **含中文的 `.ps1` 必须保存为 UTF-8 带 BOM**：Windows PowerShell 5.1 对无 BOM 的脚本按系统 ANSI（GBK）解码中文，会导致引号不闭合、**整个脚本解析失败**（`scripts/gen-cert.ps1` 曾因此完全无法运行）。改动此类脚本后务必实际执行一次验证。
- 不要在未跑 `typecheck` 的情况下发布。
